import 'package:flutter/material.dart';

/// Color tokens used by the app theme and UI components.
abstract final class CPAppColors {
  /// Primary brand color.
  static const Color primary = Color(0xFF0F766E);

  /// Accent color for emphasis actions.
  static const Color accent = Color(0xFFEAB308);

  /// Main page background color.
  static const Color background = Color(0xFFF8FAFC);

  /// Card background color.
  static const Color surface = Color(0xFFFFFFFF);

  /// Primary text color.
  static const Color textPrimary = Color(0xFF0F172A);

  /// Secondary text color.
  static const Color textSecondary = Color(0xFF475569);

  /// Soft warning color for non-blocking error states.
  static const Color warning = Color(0xFFFACC15);
}
