/// Validation error codes used for exhaustive localized mapping.
enum CPValidationErrorCode {
  invalidEmail,
  passwordTooShort,
  passwordsDoNotMatch,
  emptyField,
}

/// Authentication error codes used for exhaustive localized mapping.
enum CPAuthErrorCode {
  invalidCredentials,
  emailExists,
  authRequired,
}
